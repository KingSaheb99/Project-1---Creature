/*class Food
{
  int safeDistance = 300;
  PVector position, mousePos;
  float sizeValue;
  boolean alive = true;
  
  Food(float x, float y)
  {
   position = new PVector(x, y);
   sizeValue = random(10, 15);
  }
  
  void update()
  {
   mousePos = new PVector(mouseX, mouseY); 
  }
  
  void draw()
  {
   ellipseMode(CENTER);
   noStroke();
   fill(0);
   ellipse(mouseX, mouseY, sizeValue, sizeValue);
  }
  
  void run()
  {
    update();
    draw();
  }
}*/
